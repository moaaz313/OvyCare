import 'package:flutter/material.dart';
class TestpcodView extends StatelessWidget {
  const TestpcodView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title:Text("Test PCOD"),
        centerTitle: true,
      ),
    );
  }
}
